title: ResumeLink
url: "https://resumelink.cc"
baseurl: ""

plugins:
  - jekyll-sitemap

collections:
  roles:
    output: true

defaults:
  - scope:
      path: "roles"
    values:
      layout: "role"

permalink: /:path/
