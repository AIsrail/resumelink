title: ResumeLink
url: "https://resumelink.cc"

# Принудительно заставляем GitHub зайти в папку _roles
include:
  - _roles

collections:
  roles:
    output: true
    permalink: /roles/:name/

defaults:
  - scope:
      path: ""
      type: roles
    values:
      layout: role
