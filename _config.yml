title: ResumeLink
url: "https://resumelink.cc"

collections:
  roles:
    output: true
    permalink: /roles/:name/

include:
  - _roles

defaults:
  - scope:
      path: ""
      type: roles
    values:
      layout: role
